{% extends '@App/default/_form_modal.html.twig' %}

{% block modal_title %}
    {# Ugly hack to change the generic title on timesheet_create modal #}
    {% if title == 'Create' or 'Opret' %}
        {{ 'timesheet.create'|trans }}
    {% else %}
        {{ title }}
    {% endif %}
{% endblock %}
